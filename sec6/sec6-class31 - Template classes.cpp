// Overloading the Deference operator

/*
	Notas:
	Las plantillas o template son clases que dan problemas a la hora de la compilación. c++ utiliza las clases con los tipos concretos, por ejemplo, crea un vector de strings, a pesar de que vector es una clase plantilla que funciona con cualquier clase. Por eso necesita la definición e implementación todo en el mismo sitio. Por eso es mejor definir las clases plantilla en el fichero cabecera.
*/

#include <iostream>

using namespace std;

int main() {

	return 0;
}